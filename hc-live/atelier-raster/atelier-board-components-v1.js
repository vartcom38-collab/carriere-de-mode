/* Haute Couture Live — Atelier board components v1
   Couche additive : permet d'ajouter les éléments connus de la Bibliothèque à la planche
   et maintient une représentation structurée des composants sans remplacer le workflow existant.
*/
(function(){
'use strict';
if(window.HCAtelierBoardComponents)return;
const BOARD_KEY='haute-couture-atelier-board-v2',COMP_KEY='haute-couture-atelier-components-v1';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch(e){return f}};
const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));return true}catch(e){return false}};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let lastLibraryId=null,z=50;
function C(){return window.HCAtelierCatalog||null}
function known(id){return !!window.HCAtelierLibraryUI?.isKnown?.(id)}
function toast(t){const n=document.getElementById('toast');if(!n)return;n.textContent=t;n.classList.add('show');setTimeout(()=>n.classList.remove('show'),1500)}
function css(){if(document.getElementById('hcBoardComponentsStyle'))return;const s=document.createElement('style');s.id='hcBoardComponentsStyle';s.textContent=`
.hcl-add-board{width:100%;margin-top:12px;border:0;border-radius:12px;padding:12px;background:#211a16;color:#fff;font:900 9px Arial;letter-spacing:.05em;cursor:pointer}.hcl-add-board[disabled]{opacity:.42;cursor:not-allowed}.hc-cat-card{width:164px!important;padding:9px!important;background:#fffdf9!important;border:1px solid #e4d7cd!important;border-radius:13px!important}.hc-cat-card img{width:100%;height:118px;object-fit:contain;background:#faf5ef;border-radius:9px;display:block}.hc-cat-card .hc-cat-k{font:900 7px Arial;letter-spacing:.1em;color:#a77b66;margin:6px 0 3px}.hc-cat-card b{font:16px/.98 Georgia,serif!important}.hc-cat-card small{font:8px/1.4 Arial!important}.hc-cat-remove{position:absolute;right:6px;top:6px;width:24px;height:24px;border:0;border-radius:50%;background:#211a16;color:#fff;font-size:15px;cursor:pointer;z-index:3}
`;document.head.appendChild(s)}
function sourceImage(item){const r=window.HCAtelierVisualReferences?.forItem?.(item.id);return r?.image||item.referenceUrl||''}
async function resolvedImage(item){let img=sourceImage(item);if(img)return img;try{const r=await window.HCAtelierVisualResolver?.resolve?.(item.id);return r?.image||''}catch(e){return ''}}
function componentFromEl(el,i){const id=el.dataset.catalogId||null,item=id?C()?.byId?.(id):null;return{id:id||('legacy-'+i),catalogId:id,name:item?.name||el.dataset.name||el.querySelector('b')?.textContent?.trim()||'Élément',category:item?.category||el.dataset.category||'legacy',tags:item?.tags||[],tier:Number(item?.tier||el.dataset.tier||1),slots:item?.production?.compatibility?.slots||[],techniques:item?.production?.techniques||[],promptToken:item?.production?.promptToken||item?.visualQuery||'',x:parseFloat(el.style.left)||0,y:parseFloat(el.style.top)||0,order:i,source:'board'}}
function sync(){const els=[...document.querySelectorAll('#drop .pieceCard')];const components=els.map(componentFromEl);write(COMP_KEY,components);const board=read(BOARD_KEY,{});if(board&&typeof board==='object'){board.components=components;board.updatedAt=new Date().toISOString();write(BOARD_KEY,board)}window.dispatchEvent(new CustomEvent('hc-atelier-components-changed',{detail:{components}}));return components}
function movable(el){let m=false,dx=0,dy=0;el.addEventListener('pointerdown',e=>{if(e.target.closest('.hc-cat-remove'))return;m=true;const r=el.getBoundingClientRect();dx=e.clientX-r.left;dy=e.clientY-r.top;el.setPointerCapture?.(e.pointerId);el.style.zIndex=++z});el.addEventListener('pointermove',e=>{if(!m)return;const board=document.getElementById('board');if(!board)return;const r=board.getBoundingClientRect();el.style.left=Math.max(0,Math.min(r.width-el.offsetWidth,e.clientX-r.left-dx))+'px';el.style.top=Math.max(0,Math.min(r.height-el.offsetHeight,e.clientY-r.top-dy))+'px'});el.addEventListener('pointerup',()=>{if(m){m=false;sync()}})}
async function addToBoard(id){const item=C()?.byId?.(id);if(!item||!known(id))return false;const drop=document.getElementById('drop'),board=document.getElementById('board');if(!drop||!board)return false;const img=await resolvedImage(item);const count=drop.querySelectorAll('.pieceCard').length;const el=document.createElement('div');el.className='pieceCard hc-cat-card';el.dataset.move='';el.dataset.name=item.name;el.dataset.catalogId=item.id;el.dataset.category=item.category;el.dataset.tier=String(item.tier||1);el.style.left=(34+(count%4)*34)+'px';el.style.top=(60+(count%7)*58)+'px';el.style.zIndex=++z;el.innerHTML=`<button class="hc-cat-remove" type="button" title="Retirer">×</button>${img?`<img src="${esc(img)}" alt="${esc(item.name)}">`:'<div style="height:118px;border-radius:9px;background:#f5ede6;display:grid;place-items:center;font:italic 12px Georgia,serif;color:#8a786d">Référence visuelle</div>'}<div class="hc-cat-k">${esc(String(item.category||'COMPOSANT').toUpperCase())}</div><b>${esc(item.name)}</b><small>${esc((item.tags||[]).slice(0,3).join(' · ')||'Composant Atelier')}</small>`;drop.appendChild(el);el.querySelector('.hc-cat-remove').onclick=e=>{e.stopPropagation();el.remove();sync();toast(item.name+' retiré')};movable(el);sync();toast(item.name+' ajouté à la planche');window.dispatchEvent(new CustomEvent('hc-atelier-component-added',{detail:{item,element:el}}));return true}
function injectButton(detail){if(!detail||detail.querySelector('.hcl-add-board'))return;const id=lastLibraryId;if(!id)return;const item=C()?.byId?.(id);if(!item)return;const b=document.createElement('button');b.className='hcl-add-board';b.disabled=!known(id);b.textContent=known(id)?'＋ AJOUTER À MA PLANCHE':'🔒 À DÉCOUVRIR AVANT UTILISATION';b.onclick=async()=>{if(b.disabled)return;b.disabled=true;const ok=await addToBoard(id);if(ok){b.textContent='✓ AJOUTÉ À LA PLANCHE';setTimeout(()=>{b.disabled=false;b.textContent='＋ AJOUTER À MA PLANCHE'},1200)}else b.disabled=false};const sheet=detail.querySelector('.hcl-sheet');sheet?.appendChild(b)}
function observeLibrary(){document.addEventListener('click',e=>{const card=e.target.closest?.('[data-item]');if(card?.dataset.item)lastLibraryId=card.dataset.item},true);new MutationObserver(ms=>{for(const m of ms)for(const n of m.addedNodes){if(!(n instanceof HTMLElement))continue;if(n.classList?.contains('hcl-detail'))injectButton(n);n.querySelectorAll?.('.hcl-detail').forEach(injectButton)}}).observe(document.body,{childList:true,subtree:true})}
function observeBoard(){const drop=document.getElementById('drop');if(!drop)return;new MutationObserver(()=>setTimeout(sync,0)).observe(drop,{childList:true,subtree:false});sync()}
function boot(){if(!C()||!document.getElementById('drop')){setTimeout(boot,80);return}css();observeLibrary();observeBoard();window.HCAtelierBoardComponents={version:1,addToBoard,sync,components:()=>read(COMP_KEY,[]),key:COMP_KEY};window.dispatchEvent(new CustomEvent('hc-atelier-board-components-ready',{detail:{version:1}}))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,250));else setTimeout(boot,250);
})();
/* Haute Couture Live — Bibliothèque Atelier UI v1
   Couche additive : navigation du grand catalogue, nouveautés, fiches, verrouillages.
*/
(function(){
'use strict';
if(window.HCAtelierLibraryUI)return;
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch(e){return f}};
const UNLOCK='haute-couture-atelier-unlocks-v1',HIST='haute-couture-atelier-discovery-history-v1';
function boot(){
 const C=window.HCAtelierCatalog;if(!C){setTimeout(boot,80);return}
 if(document.getElementById('hcLibBtn'))return;
 const starter=new Set([...(C.starterIds||[])]),unlocked=()=>{const r=read(UNLOCK,[]);return new Set((Array.isArray(r)?r:[]).map(x=>typeof x==='string'?x:x.id).filter(Boolean))};
 const isKnown=id=>starter.has(id)||unlocked().has(id);
 const history=()=>read(HIST,[]);
 const catLabel=c=>C.categories?.[c]||c;
 const tierLabel=t=>['','BASE','INTERMÉDIAIRE','AVANCÉ','EXPERT'][Number(t)||1]||'SPÉCIAL';
 const style=document.createElement('style');style.id='hcAtelierLibraryStyle';style.textContent=`
 #hcLibBtn{position:fixed;z-index:8900;right:20px;bottom:20px;border:0;border-radius:999px;background:#211a16;color:#fff;padding:13px 17px;font:900 10px Arial;letter-spacing:.08em;box-shadow:0 12px 30px #0003;cursor:pointer}
 #hcLibBtn b{display:inline-grid;place-items:center;min-width:22px;height:22px;padding:0 6px;margin-left:7px;border-radius:999px;background:#df7b64;color:#fff;font-size:9px}
 #hcLibOverlay{position:fixed;inset:0;z-index:9100;background:#211915a8;display:none;place-items:center;padding:18px}#hcLibOverlay.open{display:grid}
 .hcl-shell{width:min(1380px,98vw);height:min(900px,94vh);background:#f7efe7;border:1px solid #e3d3c7;border-radius:28px;overflow:hidden;box-shadow:0 30px 90px #0006;display:grid;grid-template-columns:290px minmax(0,1fr)}
 .hcl-side{background:#211a16;color:#fff;padding:22px;overflow:auto}.hcl-side h2{font:34px/.95 Georgia,serif;margin:4px 0 7px}.hcl-side p{font:11px/1.55 Georgia,serif;color:#d7c8be}.hcl-count{font:900 9px Arial;letter-spacing:.12em;color:#e69a82}.hcl-tabs{display:grid;gap:7px;margin:18px 0}.hcl-tab{border:1px solid #ffffff22;background:#ffffff08;color:#fff;border-radius:12px;padding:11px;text-align:left;font:900 9px Arial;cursor:pointer}.hcl-tab.on{background:#fff;color:#211a16}.hcl-close{width:100%;border:0;border-radius:12px;padding:11px;background:#f0e4da;color:#211a16;font-weight:900;cursor:pointer}
 .hcl-main{display:grid;grid-template-rows:auto 1fr;min-width:0}.hcl-top{padding:18px 20px;border-bottom:1px solid #e3d3c7;background:#fffaf5;display:grid;grid-template-columns:minmax(220px,1fr) auto;gap:12px;align-items:center}.hcl-search{width:100%;border:1px solid #decfc3;border-radius:14px;padding:12px 14px;background:#fff;font:12px Arial}.hcl-filter{border:1px solid #decfc3;border-radius:12px;background:#fff;padding:10px;font-size:10px}.hcl-content{overflow:auto;padding:18px}.hcl-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px}.hcl-card{border:1px solid #e3d5ca;background:#fffdf9;border-radius:17px;padding:12px;cursor:pointer;min-height:155px;display:flex;flex-direction:column;box-shadow:0 7px 18px #70503a0d}.hcl-card.locked{opacity:.55;filter:saturate(.65)}.hcl-k{font:900 7px Arial;letter-spacing:.12em;color:#a9806c}.hcl-card h3{font:21px/.98 Georgia,serif;margin:7px 0}.hcl-tags{display:flex;gap:4px;flex-wrap:wrap;margin-top:auto}.hcl-tag{font:700 7px Arial;border:1px solid #eadfd7;border-radius:999px;padding:4px 6px;background:#fff}.hcl-state{margin-top:7px;font:900 8px Arial;color:#7d6659}.hcl-state.new{color:#c75f49}.hcl-empty{padding:60px;text-align:center;font:18px Georgia,serif;color:#77665d}
 .hcl-detail{position:fixed;z-index:9200;inset:0;background:#21191599;display:grid;place-items:center;padding:20px}.hcl-sheet{width:min(820px,95vw);max-height:88vh;overflow:auto;background:#fffaf5;border-radius:24px;padding:20px;box-shadow:0 25px 70px #0005}.hcl-sheet-top{display:flex;justify-content:space-between;gap:12px}.hcl-sheet h2{font:40px/.95 Georgia,serif;margin:5px 0 10px}.hcl-x{width:38px;height:38px;border:1px solid #dfcfc2;border-radius:50%;background:#fff;font-size:22px;cursor:pointer}.hcl-facts{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin:14px 0}.hcl-fact{border:1px solid #e7d8cc;border-radius:13px;padding:10px;background:#fff}.hcl-fact small{display:block;font:900 7px Arial;letter-spacing:.12em;color:#a07864;margin-bottom:4px}.hcl-fact div{font:12px/1.45 Georgia,serif}.hcl-ref{border:1px dashed #d5bba9;border-radius:14px;padding:12px;background:#fff3ea;font:11px/1.55 Georgia,serif}.hcl-photo{width:100%;max-height:360px;object-fit:cover;border-radius:16px;margin:12px 0;background:#eee}
 @media(max-width:980px){.hcl-shell{grid-template-columns:230px 1fr}.hcl-grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:720px){.hcl-shell{grid-template-columns:1fr;height:96vh}.hcl-side{max-height:220px}.hcl-tabs{grid-template-columns:repeat(4,1fr)}.hcl-grid{grid-template-columns:repeat(2,1fr)}.hcl-top{grid-template-columns:1fr}.hcl-facts{grid-template-columns:1fr}}
 `;document.head.appendChild(style);
 const btn=document.createElement('button');btn.id='hcLibBtn';btn.innerHTML='BIBLIOTHÈQUE ATELIER <b id="hcLibNew">0</b>';document.body.appendChild(btn);
 const overlay=document.createElement('div');overlay.id='hcLibOverlay';overlay.innerHTML=`<section class="hcl-shell"><aside class="hcl-side"><div class="hcl-count" id="hclCount"></div><h2>Ma bibliothèque</h2><p>Tout ce que tu connais, tout ce que tu as découvert, et les pistes qui restent à trouver au fil de ta carrière.</p><div class="hcl-tabs"><button class="hcl-tab on" data-view="known">MES ÉLÉMENTS</button><button class="hcl-tab" data-view="starter">MES BASES</button><button class="hcl-tab" data-view="new">NOUVEAUTÉS</button><button class="hcl-tab" data-view="locked">À DÉCOUVRIR</button></div><button class="hcl-close" id="hclClose">FERMER</button></aside><main class="hcl-main"><div class="hcl-top"><input id="hclSearch" class="hcl-search" placeholder="Rechercher : manche, mariée, tailoring, Cannes…"><select id="hclCategory" class="hcl-filter"><option value="">Toutes les catégories</option></select></div><div class="hcl-content"><div class="hcl-grid" id="hclGrid"></div></div></main></section>`;document.body.appendChild(overlay);
 let view='known',query='',category='';
 const recentIds=()=>new Set(history().slice(0,30).map(x=>x.id));
 function updateBadge(){const n=history().filter(x=>!x.seenInLibrary).length;const el=$('#hcLibNew');if(el)el.textContent=Math.min(n,99)}
 function list(){let arr=C.items.slice();const known=unlocked(),recent=recentIds();if(view==='starter')arr=arr.filter(x=>starter.has(x.id));else if(view==='new')arr=arr.filter(x=>recent.has(x.id));else if(view==='locked')arr=arr.filter(x=>!starter.has(x.id)&&!known.has(x.id));else arr=arr.filter(x=>starter.has(x.id)||known.has(x.id));if(category)arr=arr.filter(x=>x.category===category);if(query){const q=query.toLowerCase();arr=arr.filter(x=>[x.name,catLabel(x.category),...(x.tags||[]),...(x.sources||[])].join(' ').toLowerCase().includes(q))}return arr}
 function render(){const arr=list(),known=unlocked(),recent=recentIds();$('#hclCount').textContent=`${[...starter,...known].length} ÉLÉMENTS CONNUS · ${C.items.length} AU CATALOGUE`;$('#hclGrid').innerHTML=arr.length?arr.map(x=>{const lock=!starter.has(x.id)&&!known.has(x.id),isNew=recent.has(x.id);return `<article class="hcl-card ${lock?'locked':''}" data-item="${esc(x.id)}"><div class="hcl-k">${esc(catLabel(x.category).toUpperCase())} · ${tierLabel(x.tier)}</div><h3>${esc(x.name)}</h3><div class="hcl-tags">${(x.tags||[]).slice(0,4).map(t=>`<span class="hcl-tag">${esc(t)}</span>`).join('')}</div><div class="hcl-state ${isNew?'new':''}">${lock?'🔒 À DÉCOUVRIR':isNew?'✦ NOUVELLE DÉCOUVERTE':starter.has(x.id)?'BASE DE DÉPART':'✓ DÉCOUVERT'}</div></article>`}).join(''):'<div class="hcl-empty">Aucun élément dans cette sélection.</div>';document.querySelectorAll('[data-item]').forEach(el=>el.onclick=()=>detail(el.dataset.item))}
 async function detail(id){const x=C.byId(id);if(!x)return;const known=isKnown(id),hist=history().find(h=>h.id===id),prod=window.HCAtelierProductionSchema?.forItem?.(x)||null;const ref=window.HCAtelierVisualReferences?.forItem?.(id)||null;const wrap=document.createElement('div');wrap.className='hcl-detail';wrap.innerHTML=`<article class="hcl-sheet"><div class="hcl-sheet-top"><div><div class="hcl-k">${esc(catLabel(x.category).toUpperCase())} · ${tierLabel(x.tier)}</div><h2>${esc(x.name)}</h2></div><button class="hcl-x">×</button></div><div id="hclPhotoSlot"></div><div class="hcl-facts"><div class="hcl-fact"><small>STATUT</small><div>${known?(starter.has(id)?'Base connue dès le départ':'Découvert dans ta carrière'):'Verrouillé — à découvrir'}</div></div><div class="hcl-fact"><small>ORIGINE POSSIBLE</small><div>${esc((x.sources||[]).join(' · ')||'progression')}</div></div><div class="hcl-fact"><small>USAGES</small><div>${esc((x.tags||[]).join(' · ')||'création')}</div></div><div class="hcl-fact"><small>DÉCOUVERTE</small><div>${hist?esc([hist.city,hist.placeId,hist.sourceId].filter(Boolean).join(' · ')||'carrière'):(starter.has(id)?'Kit de départ':'Non découvert')}</div></div></div><div class="hcl-ref"><b>COMPOSITION / MAGNIFIC</b><br>${esc(prod?.promptToken||x.visualQuery||x.name)}${prod?.slots?.length?`<br><br><b>SLOTS :</b> ${esc(prod.slots.join(' · '))}`:''}</div></article>`;document.body.appendChild(wrap);wrap.querySelector('.hcl-x').onclick=()=>wrap.remove();wrap.onclick=e=>{if(e.target===wrap)wrap.remove()};const slot=wrap.querySelector('#hclPhotoSlot');if(ref?.image){slot.innerHTML=`<img class="hcl-photo" src="${esc(ref.image)}" alt="${esc(x.name)}"><div class="hcl-ref">Référence réelle · ${esc(ref.provider||'source visuelle')}</div>`}else if(window.HCAtelierVisualResolver?.resolve){slot.innerHTML='<div class="hcl-ref">Recherche de référence visuelle…</div>';try{const r=await window.HCAtelierVisualResolver.resolve(id);if(r?.image)slot.innerHTML=`<img class="hcl-photo" src="${esc(r.image)}" alt="${esc(x.name)}"><div class="hcl-ref">${esc(r.license||'Référence visuelle')} · ${esc(r.author||r.provider||'Wikimedia Commons')}</div>`;else slot.innerHTML=''}catch(e){slot.innerHTML=''}}}
 function populateCats(){$('#hclCategory').innerHTML='<option value="">Toutes les catégories</option>'+Object.entries(C.categories||{}).map(([id,label])=>`<option value="${esc(id)}">${esc(label)}</option>`).join('')}
 btn.onclick=()=>{overlay.classList.add('open');try{const h=history().map(x=>({...x,seenInLibrary:true}));localStorage.setItem(HIST,JSON.stringify(h))}catch(e){}updateBadge();render()};$('#hclClose').onclick=()=>overlay.classList.remove('open');overlay.onclick=e=>{if(e.target===overlay)overlay.classList.remove('open')};document.querySelectorAll('.hcl-tab').forEach(t=>t.onclick=()=>{document.querySelectorAll('.hcl-tab').forEach(x=>x.classList.remove('on'));t.classList.add('on');view=t.dataset.view;render()});$('#hclSearch').oninput=e=>{query=e.target.value;render()};$('#hclCategory').onchange=e=>{category=e.target.value;render()};window.addEventListener('hc-atelier-unlock',()=>{updateBadge();if(overlay.classList.contains('open'))render()});populateCats();updateBadge();
 window.HCAtelierLibraryUI={open:()=>btn.click(),render,isKnown};
}
boot();
})();
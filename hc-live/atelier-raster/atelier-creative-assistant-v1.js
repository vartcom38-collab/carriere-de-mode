/* Haute Couture Live — Assistante Atelier v2 : guide + génération réelle */
(function(){'use strict';if(window.HCAtelierCreativeAssistantV1)return;
const css=document.createElement('style');css.textContent=`#hc-assist{position:fixed;z-index:2147482500;left:18px;bottom:18px;width:min(390px,calc(100vw - 36px));background:rgba(255,250,245,.97);border:1px solid #dfcfc2;border-radius:20px;box-shadow:0 20px 55px rgba(35,24,19,.22);padding:15px;color:#2b211d;font-family:Arial,sans-serif}.hca-k{font:900 8px Arial;letter-spacing:.14em;color:#8b4652}.hca-t{font:22px Georgia,serif;margin:5px 0}.hca-p{font:12px/1.5 Georgia,serif;color:#78675d}.hca-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:11px}.hca-b{border:1px solid #decfc3;background:#fff;border-radius:999px;padding:9px 11px;font:800 8px Arial;cursor:pointer}.hca-b.primary{background:#2d231f;color:white;border-color:#2d231f}.hca-b:disabled{opacity:.5;cursor:wait}.hca-note{margin-top:9px;padding:9px 10px;border-radius:11px;background:#f3eae3;font:11px/1.45 Georgia,serif;color:#695950}.hca-img{width:100%;max-height:210px;object-fit:cover;border-radius:14px;margin-top:10px;display:block}`;document.head.appendChild(css);
const box=document.createElement('aside');box.id='hc-assist';document.body.appendChild(box);
function analyse(g){const missing=[];if(!g.garmentType)missing.push('le type de pièce');if(!g.materials?.length)missing.push('la matière');if(!g.colors?.length)missing.push('la couleur');if(!g.silhouette&&!g.volume)missing.push('la silhouette ou le volume');if(missing.length)return`Avant une visualisation fidèle, précise encore ${missing.slice(0,2).join(' et ')}.`;if(g.materials?.length&&g.volume&&!g.construction)return'La matière et le volume sont définis. Vérifie encore comment la construction doit réellement tenir ce volume.';return'La création contient assez d’éléments pour être visualisée. L’image restera attachée à cette pièce dans toute ta carrière.'}
function complete(g){return !!(g.garmentType&&g.materials?.length&&g.colors?.length&&(g.silhouette||g.volume))}
function render(note=''){const api=window.HCGarmentVisualPipelineV1;if(!api)return setTimeout(()=>render(note),100);const g=api.current(),img=api.imageFor?.(g,'canonical')||g.image||'',ready=complete(g);box.innerHTML=`<div class="hca-k">ASSISTANTE ATELIER</div><div class="hca-t">${g.name}</div><div class="hca-p">${analyse(g)}</div>${img?`<img class="hca-img" src="${img}" alt="Visualisation de ${g.name.replace(/"/g,'&quot;')}">`:''}<div class="hca-actions"><button class="hca-b" id="hca-refresh">RELIRE LA CRÉATION</button><button class="hca-b primary" id="hca-visual" ${ready?'':'disabled'}>${img?'REVOIR CETTE CRÉATION':'VISUALISER MA CRÉATION'}</button>${img?'<button class="hca-b" id="hca-regen">NOUVELLE INTERPRÉTATION</button>':''}</div>${note?`<div class="hca-note">${note}</div>`:''}`;
 box.querySelector('#hca-refresh').onclick=()=>render('J’ai relu l’état actuel du vêtement.');const visual=box.querySelector('#hca-visual');if(visual&&!img)visual.onclick=()=>generate(g,false);else if(visual&&img)visual.onclick=()=>render('Cette image est l’image canonique de la pièce. Elle pourra maintenant réapparaître dans le Book, les clientes et les prises de vue.');const regen=box.querySelector('#hca-regen');if(regen)regen.onclick=()=>generate(g,true)}
async function generate(g,force){const bridge=window.HCGarmentVisualGenerationBridgeV1;if(!bridge)return render('Le pont de génération n’est pas encore chargé. Réessaie dans un instant.');render(force?'Nouvelle interprétation en cours…':'Je transforme ta fiche de création en une visualisation réaliste…');const btn=box.querySelector('#hca-visual');if(btn)btn.disabled=true;try{const r=await bridge.generate(g,'atelier',{force});render(r.reused?'La visualisation existante de cette pièce est conservée.':'La création est visualisée. Cette image appartient maintenant à ce vêtement dans ta sauvegarde.')}catch(e){render('La génération n’a pas abouti pour le moment. La création reste intacte et tu pourras réessayer plus tard.')}}
setTimeout(render,350);window.addEventListener('hc-garment-library',()=>setTimeout(()=>render(),80));window.addEventListener('hc-garment-generation-complete',()=>setTimeout(()=>render('Visualisation terminée.'),80));window.HCAtelierCreativeAssistantV1={version:2,render,analyse,generate};})();
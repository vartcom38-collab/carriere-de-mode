/* Haute Couture Live — generation hotfix v4.1
   Une seule lecture visible de la planche, une seule action de génération, aucun ancien contrôle parasite.
*/
(function(){
'use strict';
if(window.__HC_GENERATION_HOTFIX_V41__)return;window.__HC_GENERATION_HOTFIX_V41__=true;
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
function uniq(list){return [...new Set(list)]}
function norm(s){return String(s||'').replace(/\s+/g,' ').trim()}
function boardEls(){const drop=$('#drop'),board=$('#board');if(!drop||!board)return[];const all=uniq([...drop.querySelectorAll('.pieceCard,.hc-cat-card,.hc-mood-card,[data-instance-id],[data-catalog-id],[data-mood-kind]'),...board.querySelectorAll('#drop > div[data-move]')]).filter(el=>el instanceof HTMLElement&&!el.classList.contains('noteCard'));all.forEach((el,i)=>{el.classList.add('pieceCard');if(!el.dataset.instanceId)el.dataset.instanceId='live-'+Date.now()+'-'+i;if(!el.dataset.name)el.dataset.name=norm(el.dataset.referenceLabel||el.querySelector('b')?.textContent||el.querySelector('img')?.alt||el.dataset.moodId||('Élément '+(i+1)))});return all}
function kindOf(el){const k=el.dataset.moodKind||'';if(k==='material')return'materials';if(k==='color')return'colors';if(k==='pattern')return'patterns';if(k==='note'||el.querySelector('textarea'))return'notes';return'garments'}
function boardState(){const out={garments:[],materials:[],colors:[],patterns:[],notes:[]};boardEls().forEach(el=>{const k=kindOf(el);let name='';if(k==='notes')name=norm(el.querySelector('textarea')?.value||'');else name=norm(el.dataset.referenceLabel||el.dataset.name||el.querySelector('b')?.textContent||el.querySelector('img')?.alt||'Référence');if(name)out[k].push(name)});Object.keys(out).forEach(k=>out[k]=uniq(out[k]));return out}
function counts(){const els=boardEls();const c={total:els.length,garments:0,materials:0,colors:0,patterns:0,notes:0};els.forEach(el=>c[kindOf(el)]++);return c}
function badge(){let n=$('#hcv3MoodCount');const actions=$('.hcv3-actions');if(!actions)return null;if(!n){n=document.createElement('span');n.id='hcv3MoodCount';n.style.cssText='align-self:center;white-space:nowrap;font:800 8px Arial;color:#6f5e55;background:#f3e9e1;border:1px solid #e2d4ca;border-radius:999px;padding:8px 10px';actions.insertBefore(n,$('.hcv3-generate'))}return n}
function summaryBox(){let box=$('#hcv3GenerationInputs');const head=$('.hcv3-center-head');if(!head)return null;if(!box){box=document.createElement('div');box.id='hcv3GenerationInputs';box.innerHTML='<div class="hcv3-inputs-title"><b>PRIS EN COMPTE POUR LES CROQUIS</b><span>Mis à jour automatiquement depuis ta planche</span></div><div class="hcv3-inputs-grid"></div><div class="hcv3-inputs-foot">La génération utilise aussi la commande cliente et une capture visuelle de toute la planche : placement, taille, proximité et superpositions.</div>';head.appendChild(box)}return box}
function fmt(label,list,empty='Aucun'){return `<div class="hcv3-input-row"><b>${label}</b><span class="${list.length?'':'empty'}">${list.length?list.join(' · '):empty}</span></div>`}
function updateSummary(){const s=boardState(),c=counts(),n=badge();if(n){n.textContent=c.total?`${c.total} élément${c.total>1?'s':''} · ${c.garments} vêt. · ${c.materials} mat. · ${c.colors} coul. · ${c.patterns} motif${c.patterns>1?'s':''} · ${c.notes} note${c.notes>1?'s':''}`:'Planche vide';n.dataset.total=String(c.total)}const box=summaryBox();if(!box)return;box.querySelector('.hcv3-inputs-grid').innerHTML=fmt('Vêtements / formes',s.garments)+fmt('Matières',s.materials)+fmt('Couleurs',s.colors)+fmt('Motifs',s.patterns,'Aucun motif sélectionné')+fmt('Notes de planche',s.notes,'Aucune note écrite')}
function removeLegacyNoise(){['hcMoodboardToolbar','hcMoodboardToolbarFixed','hcAiSummary'].forEach(id=>document.getElementById(id)?.remove());$$('#hcLibBtn').forEach(el=>el.style.setProperty('display','none','important'));$$('body *').forEach(el=>{const t=norm(el.textContent);if(/^Bibliothèque Atelier(?:\s+\d+)?$/i.test(t)){const target=el.closest('button,a,[role="button"]')||el;if(!target.closest('#hcAtelierShellV3'))target.style.setProperty('display','none','important')}if(t==='Lecture de ta composition'){const box=el.closest('section,article,.card,.panel,[class*=composition],[class*=reading]')||el.parentElement;if(box&&box!==document.body&&!box.closest('#board')&&!box.closest('#hcv3GenerationInputs'))box.remove()}})}
function ensureRemove(){boardEls().forEach(el=>{if(el.querySelector('.hc-cat-remove,.hc-gc-remove'))return;const b=document.createElement('button');b.type='button';b.className='hc-gc-remove';b.textContent='×';b.title='Retirer';b.style.cssText='position:absolute;right:6px;top:6px;width:26px;height:26px;border:0;border-radius:50%;background:#211a16;color:#fff;z-index:9999;cursor:pointer;font:700 16px Arial';b.addEventListener('pointerdown',e=>e.stopPropagation(),true);b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();el.remove();window.HCAtelierFinalDrop?.save?.();window.HCAtelierBoardComponents?.sync?.();window.dispatchEvent(new CustomEvent('hc-atelier-moodboard-changed'));updateSummary()},true);el.appendChild(b)})}
function ensureNoteHandles(){boardEls().filter(el=>kindOf(el)==='notes').forEach(el=>{if(el.querySelector('.hc-note-drag-handle'))return;const ta=el.querySelector('textarea');if(!ta)return;const h=document.createElement('div');h.className='hc-note-drag-handle';h.textContent='NOTE · déplacer';ta.before(h)})}
function ensureNewIdea(){const pane=$('.hcv3-pane[data-pane="notes"]');if(!pane||$('#hcv3NewIdea'))return;const b=document.createElement('button');b.id='hcv3NewIdea';b.type='button';b.textContent='＋ NOUVELLE IDÉE';b.onclick=()=>{const ok=window.HCAtelierFinalDrop?.addNote?.();if(!ok)return;setTimeout(()=>{$('#drop .hc-mood-card:last-of-type textarea')?.focus();updateSummary()},30)};pane.insertBefore(b,pane.firstChild)}
function wrapControllerFetch(){const ctl=window.HCAtelierGenerationController;if(!ctl?.generate||ctl.__hcTopFetchWrapped)return;ctl.__hcTopFetchWrapped=true;const original=ctl.generate.bind(ctl);ctl.generate=async function(e){const own=window.fetch;let topFetch=null;try{if(window.top&&window.top!==window&&typeof window.top.fetch==='function')topFetch=window.top.fetch.bind(window.top)}catch(_){}if(topFetch)window.fetch=topFetch;try{return await original(e)}finally{window.fetch=own}}}
function bind(){const b=$('.hcv3-generate');if(!b)return false;b.onclick=null;if(b.dataset.hcHotfixV41==='1')return true;b.dataset.hcHotfixV41='1';b.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();updateSummary();ensureRemove();const c=counts();if(!c.total){const t=$('#toast');if(t){t.textContent='Ajoute au moins un élément à ta planche avant de générer.';t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}return false}wrapControllerFetch();const ctl=window.HCAtelierGenerationController;if(!ctl?.generate){console.error('[Atelier hotfix] generation controller unavailable');return false}return await ctl.generate(e)},true);return true}
function css(){if($('#hcv3GenerationInputsCss'))return;const s=document.createElement('style');s.id='hcv3GenerationInputsCss';s.textContent=`#hcLibBtn{display:none!important}#hcv3GenerationInputs{margin-top:10px;border:1px solid #e3d5ca;background:#fffaf5;border-radius:14px;padding:11px 12px}.hcv3-inputs-title{display:flex;align-items:baseline;gap:8px;flex-wrap:wrap;margin-bottom:7px}.hcv3-inputs-title b{font:900 8px Arial;letter-spacing:.08em;color:#6f5144}.hcv3-inputs-title span{font:8px Georgia,serif;color:#8b786e}.hcv3-inputs-grid{display:grid;gap:5px}.hcv3-input-row{display:grid;grid-template-columns:120px 1fr;gap:8px;font:9px/1.35 Arial}.hcv3-input-row b{color:#6d5a50}.hcv3-input-row span{color:#2e2520}.hcv3-input-row span.empty{color:#a09188;font-style:italic}.hcv3-inputs-foot{margin-top:8px;padding-top:7px;border-top:1px solid #eee3da;font:italic 8px/1.35 Georgia,serif;color:#7d6b61}#hcv3NewIdea{width:100%;margin:0 0 9px;border:1px solid #d9c8bb;background:#2a211c;color:#fff;border-radius:11px;padding:10px;font:900 8px Arial;letter-spacing:.06em;cursor:pointer}.hc-note-drag-handle{margin:-2px 30px 6px 0;padding:5px 7px;border-radius:7px;background:#f0dfad;color:#6b5736;font:900 7px Arial;letter-spacing:.08em;cursor:move;user-select:none}`;document.head.appendChild(s)}
function scan(){removeLegacyNoise();ensureRemove();ensureNoteHandles();ensureNewIdea();updateSummary();wrapControllerFetch();bind()}
function boot(){css();scan();new MutationObserver(()=>setTimeout(scan,0)).observe(document.body,{childList:true,subtree:true});window.addEventListener('hc-atelier-moodboard-changed',updateSummary);window.addEventListener('input',e=>{if(e.target.closest?.('#drop'))updateSummary()},true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,1700));else setTimeout(boot,1700);
})();
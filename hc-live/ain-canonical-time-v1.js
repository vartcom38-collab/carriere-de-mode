/* Compatibility shim only.
   Ain canonical time is handled by ain-parity-system-bridge-v1.js.
   Kept temporarily because game-engine may request this asset from cached pages. */
(function(){'use strict';window.__HCAinCanonicalTimeV1=true})();

/* Haute Couture Live — tableau de commandes génériques · Nîmes v1
   Couche additive au Café des Croquis. N'altère pas la commande Éloïse.
*/
(function(){
'use strict';
if(window.__HC_NIMES_ORDER_BOARD__)return;window.__HC_NIMES_ORDER_BOARD__=true;
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function E(){return window.HCClientOrderEngine||null}function F(){return window.HCClientFittingEngine||null}
function fmtDate(iso){try{return new Intl.DateTimeFormat('fr-FR',{weekday:'short',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(iso))}catch(e){return iso||''}}
function css(){if($('#hcOrderBoardCss'))return;const s=document.createElement('style');s.id='hcOrderBoardCss';s.textContent=`
.hcob{margin:16px 0;padding:15px;border:1px solid #e4d4c8;border-radius:21px;background:#fffaf5}.hcob-top{display:flex;justify-content:space-between;gap:12px;align-items:flex-end}.hcob-k{font:900 8px Arial;letter-spacing:.16em;color:#b26f5f}.hcob h3{font:27px/.98 Georgia,serif;margin:5px 0}.hcob-sub{font:11px/1.5 Georgia,serif;color:#6b5d55}.hcob-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:12px}.hcob-card{border:1px solid #e7dbd2;border-radius:16px;padding:13px;background:#fff;box-shadow:0 7px 18px #6c4d3710}.hcob-card h4{font:22px Georgia,serif;margin:4px 0}.hcob-role{font:9px Arial;color:#806f65}.hcob-quote{margin:9px 0;padding:9px;border-radius:10px;background:#f8ede6;font:italic 11px/1.45 Georgia,serif}.hcob-facts{display:grid;grid-template-columns:86px 1fr;gap:5px 8px;font:9px/1.35 Arial;border-top:1px solid #eee3db;padding-top:8px}.hcob-facts b{font-size:7px;letter-spacing:.08em}.hcob-actions{display:flex;gap:7px;margin-top:10px}.hcob-btn{flex:1;border:0;border-radius:10px;padding:10px;background:#211a16;color:#fff;font:900 8px Arial}.hcob-btn.alt{background:#eadfd5;color:#211a16}.hcob-note{margin-top:10px;border:1px dashed #dac2b4;border-radius:11px;padding:9px;font:10px/1.45 Georgia,serif}.hcob-active{background:#f0f4ed;border-color:#cfdcc9}.hcob-active .hcob-k{color:#6b8063}@media(max-width:700px){.hcob-grid{grid-template-columns:1fr}}
`;document.head.appendChild(s)}
function host(){const body=$('#npBody');if(!body)return null;return body}
function offerCard(o){return `<article class="hcob-card"><div class="hcob-k">NOUVELLE DEMANDE · ${esc(o.difficulty)}</div><h4>${esc(o.clientName)}</h4><div class="hcob-role">${esc(o.clientRole)} · personnage fictif</div><div class="hcob-quote">« ${esc(o.notes)} »</div><div class="hcob-facts"><b>PIÈCE</b><span>${esc(o.garment)}</span><b>OCCASION</b><span>${esc(o.occasion)}</span><b>STYLE</b><span>${esc(o.brief?.style||'')}</span><b>MATIÈRES</b><span>${esc((o.brief?.materialsPreferred||[]).join(' · '))}</span><b>BUDGET</b><span>${Number(o.budget||0)} €</span><b>RÉMUNÉRATION</b><span>${Number(o.reward||0)} €</span><b>DÉLAI</b><span>${esc(fmtDate(o.deadline))}</span><b>TRAVAIL</b><span>≈ ${Math.round(Number(o.estimatedMinutes||0)/60*10)/10} h</span></div><div class="hcob-actions"><button class="hcob-btn" data-hc-order-accept="${esc(o.id)}">ACCEPTER</button><button class="hcob-btn alt" data-hc-order-decline="${esc(o.id)}">REFUSER</button></div></article>`}
function activeCard(o){return `<article class="hcob-card hcob-active"><div class="hcob-k">COMMANDE ACTIVE</div><h4>${esc(o.clientName)}</h4><div class="hcob-role">${esc(o.garment)}</div><div class="hcob-quote">${esc(o.occasion)}</div><div class="hcob-facts"><b>STATUT</b><span>${esc(o.status)}</span><b>DÉLAI</b><span>${esc(fmtDate(o.deadline))}</span><b>RÉMUNÉRATION</b><span>${Number(o.reward||0)} €</span></div>${o.status==='accepted'?'<div class="hcob-actions"><button class="hcob-btn" data-hc-open-atelier>OUVRIR L’ATELIER</button></div>':''}</article>`}
function render(){const body=host(),e=E();if(!body||!e||!body.textContent.includes('Café des Croquis'))return;css();e.expire();let box=body.querySelector('#hcGenericOrderBoard');if(!box){box=document.createElement('section');box.id='hcGenericOrderBoard';box.className='hcob';body.appendChild(box)}const active=e.active();const offers=active?[]:e.offers({city:'Nîmes',source:'Café des Croquis',count:2});box.innerHTML=`<div class="hcob-top"><div><div class="hcob-k">CARNET DE COMMANDES · RÉSEAU LOCAL</div><h3>Opportunités clientes</h3><div class="hcob-sub">Les demandes évoluent avec ta réputation. Un brief accepté rejoint automatiquement ton Atelier, ton Agenda et le système d’essayage.</div></div><div class="hcob-k">NIVEAU CARRIÈRE ${e.careerLevel()}/4</div></div><div class="hcob-grid">${active?activeCard(active):offers.length?offers.map(offerCard).join(''):'<div class="hcob-note">Aucune nouvelle demande aujourd’hui. Reviens un autre jour ou développe ton réseau local.</div>'}</div><div id="hcobFeedback"></div>`}
function feedback(msg){const f=$('#hcobFeedback');if(f)f.innerHTML=`<div class="hcob-note">${esc(msg)}</div>`}
function click(ev){const a=ev.target.closest?.('[data-hc-order-accept]'),d=ev.target.closest?.('[data-hc-order-decline]'),open=ev.target.closest?.('[data-hc-open-atelier]');if(a){ev.preventDefault();const r=E()?.accept(a.dataset.hcOrderAccept);if(!r?.ok)feedback(r?.error||'Impossible');else{feedback('Commande acceptée. Le brief est maintenant dans ton Atelier et les rendez-vous sont dans ton Agenda.');render()}}else if(d){ev.preventDefault();const r=E()?.decline(d.dataset.hcOrderDecline);if(!r?.ok)feedback(r?.error||'Impossible');else render()}else if(open){ev.preventDefault();try{window.top.location.href='../atelier/'}catch(e){location.href='../atelier/'}}}
let queued=false;function schedule(){if(queued)return;queued=true;setTimeout(()=>{queued=false;render()},80)}
function boot(){const body=host();if(!body){setTimeout(boot,120);return}body.addEventListener('click',click);new MutationObserver(schedule).observe(body,{childList:true,subtree:true});window.addEventListener('hc-client-order',schedule);window.addEventListener('hc-client-delivered',schedule);render()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,350));else setTimeout(boot,350);
})();
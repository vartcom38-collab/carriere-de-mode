/* Haute Couture Live — portée géographique de carrière v1
   Extension additive : transforme la réputation spécialisée en pistes professionnelles hors de la ville actuelle.
   N'ouvre pas de ville non produite ; mémorise seulement l'horizon professionnel et les futurs motifs de déplacement.
*/
(function(){
'use strict';
if(window.__HC_CAREER_GEOGRAPHY__)return;window.__HC_CAREER_GEOGRAPHY__=true;
const TERRITORY_KEY='haute-couture-territorial-world-v1';
const TRACKS={tailoring:'Tailoring',bridal:'Bridal',ceremonie:'Cérémonie',scene:'Scène',redCarpet:'Red carpet',pageant:'Pageant',avantGarde:'Avant-garde',upcycling:'Upcycling',editorial:'Éditorial'};
const MID_CITY={tailoring:'Lyon',bridal:'Marseille',ceremonie:'Marseille',scene:'Marseille',redCarpet:'Marseille',pageant:'Lyon',avantGarde:'Lyon',upcycling:'Lyon',editorial:'Lyon'};
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch(e){return f}};
const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));return true}catch(e){return false}};
const slug=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
function game(){try{return window.HCGame?.get?.()||null}catch(e){return null}}
function contactFor(g,track){const list=(g?.professionalNetwork||[]).filter(x=>x.specialty===track);return list.sort((a,b)=>(b.firstContactDay||0)-(a.firstContactDay||0))[0]||null}
function cityFor(track,threshold){if(threshold===6)return'Montpellier';if(threshold===10)return MID_CITY[track]||'Lyon';return'Paris'}
function tierFor(threshold){return threshold>=16?'national':threshold>=10?'interrégional':'régional'}
function territorialUnlock(city,track,threshold){const s=read(TERRITORY_KEY,{version:1,places:{},encounters:{},knownPeople:{},rumors:[],unlocks:{}});s.unlocks=s.unlocks||{};const key='career-city-'+slug(city);if(!s.unlocks[key])s.unlocks[key]={source:'career-reputation',city,track,threshold,day:game()?.clock?.day||1};write(TERRITORY_KEY,s);window.dispatchEvent(new CustomEvent('hc-territory-state',{detail:s}))}
function registerLead(track,score,threshold){const g=game(),label=TRACKS[track];if(!g||!label)return;const city=cityFor(track,threshold),id=`career-geo-${track}-${threshold}`,contact=contactFor(g,track);if(g.flags?.careerGeographyLedger?.[id])return;
 window.HCGame?.mutate?.(s=>{s.flags=s.flags||{};s.flags.careerGeographyLedger=s.flags.careerGeographyLedger||{};if(s.flags.careerGeographyLedger[id])return;s.flags.careerGeographyLedger[id]=s.clock?.iso||new Date().toISOString();s.careerTravelLeads=s.careerTravelLeads||[];const lead={id,city,scope:tierFor(threshold),specialty:track,specialtyLabel:label,specialtyScore:score,threshold,createdDay:s.clock?.day||1,createdAt:s.clock?.iso||new Date().toISOString(),sourceContact:contact?{name:contact.name,role:contact.role}:null,status:'piste professionnelle',playable:false,reason:threshold>=16?`Ton nom en ${label} commence à circuler à l'échelle nationale.`:threshold>=10?`Ton réseau ${label} dépasse désormais ton bassin régional.`:`Une première piste ${label} apparaît hors de Nîmes.`};if(!s.careerTravelLeads.some(x=>x.id===id))s.careerTravelLeads.unshift(lead);s.messages=s.messages||[];const from=contact?.name||'Réseau professionnel',text=threshold>=16?`Une piste sérieuse arrive de Paris autour de ton travail en ${label}. Rien n'est encore planifié, mais ton nom commence à entrer dans des réseaux nationaux.`:threshold>=10?`Ton travail en ${label} remonte maintenant jusqu'à ${city}. Un contact de ton réseau pense qu'une opportunité pourrait s'y ouvrir.`:`Ton nom a circulé jusqu'à Montpellier grâce à ton travail en ${label}. C'est la première fois qu'une piste professionnelle claire apparaît hors de Nîmes.`;if(!s.messages.some(m=>m.id===id))s.messages.unshift({id,from,avatar:String(from)[0]||'R',subject:`Une piste à ${city}`,text,receivedAt:s.clock?.iso||new Date().toISOString(),read:false,action:null});});territorialUnlock(city,track,threshold);window.dispatchEvent(new CustomEvent('hc-career-geography',{detail:{track,score,threshold,city}}))}
function sync(){const g=game();if(!g)return;const r=g.reputationTracks||{};for(const track of Object.keys(TRACKS)){const score=Math.max(0,Number(r[track]||0));if(score>=6)registerLead(track,score,6);if(score>=10)registerLead(track,score,10);if(score>=16)registerLead(track,score,16)}}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function injectUI(){const qs=new URLSearchParams(location.search);if((qs.get('app')||'home')!=='carriere')return false;const wrap=document.querySelector('.hcc-wrap');if(!wrap||document.getElementById('hcc-geography'))return false;const g=game()||{},leads=(g.careerTravelLeads||[]).slice().sort((a,b)=>(b.threshold||0)-(a.threshold||0)||(b.createdDay||0)-(a.createdDay||0));const section=document.createElement('section');section.id='hcc-geography';section.className='hcc-section';section.innerHTML=`<h2>Portée du réseau</h2>${leads.length?leads.map(x=>`<article class="hcc-domain"><div class="hcc-head"><b>${esc(x.city)}</b><span class="hcc-badge">${esc(x.scope)} · ${esc(x.specialtyLabel)}</span></div><p>${esc(x.reason)}${x.sourceContact?` ${esc(x.sourceContact.name)} (${esc(x.sourceContact.role)}) est lié à cette piste.`:''}</p></article>`).join(''):`<div class="hcc-empty">Pour l'instant, ta carrière reste ancrée à Nîmes. Une spécialité établie commencera à faire apparaître des pistes à Montpellier, puis dans d'autres villes.</div>`}<div class="hcc-note">Une ville qui apparaît ici entre dans ton horizon professionnel. Le déplacement ne sera proposé que lorsqu'une opportunité concrète et le contenu de cette destination seront prêts.</div>`;const note=wrap.querySelector('.hcc-note');if(note)wrap.insertBefore(section,note);else wrap.appendChild(section);return true}
function boot(){let tries=0;const tick=()=>{tries++;if(window.HCGame){sync();if(injectUI())return}if(tries<120)setTimeout(tick,50)};tick()}
boot();
window.addEventListener('hc-game-state',()=>setTimeout(()=>{sync();injectUI()},0));
window.addEventListener('hc-specialty-reputation',()=>setTimeout(()=>{sync();injectUI()},0));
})();
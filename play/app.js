const screens=[...document.querySelectorAll('.screen')];const modal=document.querySelector('#modal');const transition=document.querySelector('.transition');function show(name){transition.classList.remove('go');void transition.offsetWidth;transition.classList.add('go');setTimeout(()=>{screens.forEach(s=>s.classList.toggle('active',s.dataset.screen===name));},280)}function toast(text){const t=document.createElement('div');t.className='toast';t.textContent=text;document.body.appendChild(t);setTimeout(()=>t.remove(),1800)}document.addEventListener('click',e=>{const a=e.target.closest('[data-action]');if(!a)return;const action=a.dataset.action;if(action==='newgame')show('prologue');if(action==='home')show('home');if(action==='prologue')show('prologue');if(action==='characters')show('characters');if(action==='continue')toast('Aucune sauvegarde pour le moment');if(action==='options'){document.querySelector('#modal-title').textContent='Options';document.querySelector('#modal-text').textContent='Les réglages son, musique et affichage seront ajoutés ici.';modal.showModal()}if(action==='close')modal.close()});document.querySelectorAll('.character').forEach(card=>card.addEventListener('click',()=>{document.querySelectorAll('.character').forEach(c=>c.classList.remove('selected'));card.classList.add('selected');const confirm=document.querySelector('.screen-characters .btn.primary');confirm.disabled=false;confirm.textContent='Confirmer ce personnage';}));
// preview reset v1
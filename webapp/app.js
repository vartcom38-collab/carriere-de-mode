const screens=[...document.querySelectorAll('[data-screen]')];const dialog=document.querySelector('#optionsDialog');function show(name){screens.forEach(s=>s.classList.toggle('active',s.dataset.screen===name));if(name==='home'){history.replaceState(null,'',location.pathname)}else{location.hash=name}localStorage.setItem('hc_screen',name)}function route(){const hash=location.hash.replace('#','');if(hash&&screens.some(s=>s.dataset.screen===hash)){show(hash)}else{show('home')}}document.addEventListener('click',e=>{const el=e.target.closest('[data-action]');if(!el)return;const action=el.dataset.action;if(action==='newgame'||action==='prologue')show('prologue');else if(action==='home')show('home');else if(action==='characters')show('characters');else if(action==='continue'){const saved=localStorage.getItem('hc_screen');show(saved&&saved!=='home'?saved:'prologue')}else if(action==='options')dialog.showModal()});window.addEventListener('hashchange',route);route();
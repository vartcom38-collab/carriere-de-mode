name: Disabled spatial housing discovery one-shot
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled after successful spatial housing loader patch."

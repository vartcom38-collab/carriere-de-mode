name: Deploy cinematic checkpoint

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  checkpoint_disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Checkpoint manuel uniquement — la preview officielle est gérée par live-preview.yml"

name: Disabled legacy logement startup repair
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled after successful legacy logement startup repair."

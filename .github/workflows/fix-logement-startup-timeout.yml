name: Disabled legacy logement geography fix
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled after successful legacy logement geography repair."
